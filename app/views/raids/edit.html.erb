<head>
    <meta charset="utf-8">
</head>
<h1>レイド編集</h1>
<body>
    <%= form_tag("update") do %>
        <p>
          ポケモン<br>
          <%= collection_select(@pokemon, :pokemon_id, @pokemons, :id, :name) %><br>
          場所<br>
          <%= collection_select(@place, :place_id, @places, :id, :name) %><br>
          時間<br>
          <input type="datetime-local" name="date"><%= @raid.date %></input><br>
          一言<br>
          <textarea name="comment"><%= @raid.comment %></textarea><br>
          <input type="submit" value="保存する">
        </p>
    <% end %>
</body>
